const mongoose = require('mongoose'); async function test() { try { await mongoose.connect('mongodb://localhost:27017/test_ludo'); const schema = new mongoose.Schema({ name: String }); const Model = mongoose.model('Test', schema); await Model.create({ name: 'test' }); await Model.findOneAndUpdate({}, { name: 'test2' }, { session: null }); console.log('Success'); } catch(e) { console.error('Error:', e.message); } process.exit(0); } test();
