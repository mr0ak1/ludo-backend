/** Cluster 9 — Player Stats & Leaderboard */

const LEADERBOARD_TOP_N = 100;
const LEADERBOARD_MIN_GAMES = 10;
const LEADERBOARD_CACHE_TTL_MS = 60 * 60 * 1000; // hourly refresh

const RANK_POINTS_WIN = 30;
const RANK_POINTS_LOSS = 8;

const MATCH_HISTORY_DEFAULT_PAGE = 1;
const MATCH_HISTORY_DEFAULT_LIMIT = 20;
const MATCH_HISTORY_MAX_LIMIT = 50;

const TOKEN_COLORS = ['red', 'green', 'yellow', 'blue'];

module.exports = {
  LEADERBOARD_TOP_N,
  LEADERBOARD_MIN_GAMES,
  LEADERBOARD_CACHE_TTL_MS,
  RANK_POINTS_WIN,
  RANK_POINTS_LOSS,
  MATCH_HISTORY_DEFAULT_PAGE,
  MATCH_HISTORY_DEFAULT_LIMIT,
  MATCH_HISTORY_MAX_LIMIT,
  TOKEN_COLORS,
};
